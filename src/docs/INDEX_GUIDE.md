# 📋 索引系统 - 详细使用指南

## 概述

这个索引系统允许你通过编辑 `index.json` 文件来管理网站上的所有条目。系统会自动加载数据并提供搜索、过滤、标签等功能。

## 文件位置

```
src/
├── index.json              ← 编辑这个文件来添加/更新条目
├── index.html              ← 索引页面
├── js/
│   └── index-manager.js    ← 动态加载脚本
└── docs/
    └── INDEX_GUIDE.md      ← 你在这里
```

## 快速开始

### 1. 编辑 `src/index.json`

```json
{
  "items": [
    {
      "id": 1,
      "title": "条目标题",
      "description": "条目描述",
      "type": "posts",
      "url": "/path",
      "date": "2025-12-25",
      "tags": ["tag1"]
    }
  ],
  "recent": [
    {
      "title": "更新标题",
      "icon": "A",
      "time": "3天前",
      "url": "/path"
    }
  ]
}
```

### 2. 提交更改

```bash
git add src/index.json
git commit -m "feat: 添加新条目"
git push origin main
```

### 3. 刷新网页

访问并刷新 https://Xwdjjy.github.io/src/index.html

## 字段详解

### items 数组

#### 必需字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | number | 唯一ID，不能重复 |
| `title` | string | 条目标题 |
| `description` | string | 条目描述 |
| `type` | string | 类型：`posts`、`projects` 或 `docs` |
| `url` | string | 点击条目时跳转的链接 |

#### 可选字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `date` | string | 发布日期（格式：YYYY-MM-DD） |
| `meta` | string | 额外信息（如版本、分类等） |
| `tags` | array | 搜索标签数组 |

### recent 数组

| 字段 | 类型 | 说明 |
|------|------|------|
| `title` | string | 更新标题 |
| `icon` | string | 单个字符头像 |
| `time` | string | 时间描述（如"3天前"） |
| `url` | string | 点击时跳转的链接 |

## 条目类型

### 📝 posts（文章）

用于博客文章、教程、技术文章等。

```json
{
  "id": 1,
  "title": "React 性能优化指南",
  "description": "深入讲解如何优化 React 应用性能...",
  "type": "posts",
  "url": "/blog/react-optimization",
  "date": "2025-12-15",
  "tags": ["react", "performance"]
}
```

### 🚀 projects（项目）

用于代码项目、个人作品、开源项目等。

```json
{
  "id": 2,
  "title": "响应式作品集模板",
  "description": "轻量、可定制的作品集模板...",
  "type": "projects",
  "url": "/projects/portfolio",
  "meta": "开源",
  "tags": ["design", "frontend"]
}
```

### 📚 docs（文档）

用于 API 文档、使用手册、指南等。

```json
{
  "id": 3,
  "title": "JavaScript 基础教程",
  "description": "从零开始学习 JavaScript...",
  "type": "docs",
  "url": "/docs/js-basics",
  "meta": "v1.0",
  "tags": ["javascript", "tutorial"]
}
```

## 工作流程

```
1. 编辑 src/index.json
   ↓
2. 添加条目到 items 数组
   ↓
3. 可选：更新 recent 数组
   ↓
4. git add src/index.json
   ↓
5. git commit -m "feat: 描述"
   ↓
6. git push origin main
   ↓
7. GitHub Pages 自动发布
   ↓
8. 访问网页并刷新 (Ctrl+F5)
   ↓
9. 新条目显示在页面上
```

## 常见问题

### Q: 为什么页面没有更新？

**A:** 检查以下几点：

1. JSON 格式是否正确
   - 使用 [JSONLint](https://jsonlint.com/) 验证
   - 检查是否有多余的逗号或引号

2. 是否已推送到 GitHub
   - 运行 `git push origin main`

3. 浏览器缓存
   - 刷新页面 (Ctrl+F5)
   - 清除缓存 (Ctrl+Shift+Delete)

### Q: 如何删除条目？

**A:** 从 `items` 数组中删除对应的条目对象，然后推送更改：

```bash
git add src/index.json
git commit -m "feat: 删除旧条目"
git push origin main
```

### Q: 可以使用外部链接吗？

**A:** 可以。`url` 字段支持：

- 相对路径：`/blog/post-1`
- 绝对URL：`https://example.com/page`
- 锚点：`#section`

### Q: 如何自定义标签？

**A:** `tags` 字段是数组，可以填任何值。搜索时会在标题、描述和标签中查找匹配项：

```json
{
  "tags": ["自定义标签1", "tag2", "tag3"]
}
```

### Q: 能否更改类型的显示名称？

**A:** 不能直接更改，但可以修改 `src/js/index-manager.js` 中的 `getTypeName` 方法：

```javascript
getTypeName(type) {
  const names = {
    posts: '博文',        // 修改这里
    projects: '项目',
    docs: '文档'
  };
  return names[type] || '条目';
}
```

## 高级用法

### 按日期排序

虽然索引系统不自动排序，但建议按日期降序排列：

```json
{
  "items": [
    { "id": 1, "title": "最新文章", "date": "2025-12-25" },
    { "id": 2, "title": "旧文章", "date": "2025-06-01" }
  ]
}
```

### 使用 meta 字段标记版本或分类

```json
{
  "id": 1,
  "title": "API 文档",
  "meta": "v2.0",
  "type": "docs"
}
```

### 添加多个标签便于搜索

```json
{
  "tags": ["javascript", "react", "hooks", "tutorial"]
}
```

## 最佳实践

### ✅ 推荐做法

```json
{
  "id": 1,
  "title": "深入理解 JavaScript 闭包",
  "description": "详细讲解闭包工作原理、常见用法、性能影响等",
  "type": "posts",
  "url": "/blog/closure-guide",
  "date": "2025-12-15",
  "meta": "进阶教程",
  "tags": ["javascript", "closure", "advanced"]
}
```

### ❌ 避免做法

```json
{
  "id": 2,
  "title": "文章",                    // ❌ 标题太笼统
  "description": "这是一篇文章",      // ❌ 描述太简洁
  "type": "post",                    // ❌ 类型应该是 posts
  "url": "#"                         // ❌ URL 无效
}
```

## 性能提示

- 保持 JSON 文件大小合理（建议 < 100KB）
- 条目过多时考虑添加分页功能
- 定期清理不需要的旧条目

## 支持和帮助

- 快速参考：../QUICK_REFERENCE.md
- 工作流程：./WORKFLOW_DIAGRAM.md
- 实现细节：./IMPLEMENTATION_SUMMARY.md
- 部署上线：./COMPLETION_CHECKLIST.md

---

**最后更新**: 2026-02-08  
**版本**: 1.0.0
